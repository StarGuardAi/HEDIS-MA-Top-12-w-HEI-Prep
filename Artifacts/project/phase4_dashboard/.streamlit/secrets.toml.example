# Streamlit Secrets Configuration Template
# Copy this file to .streamlit/secrets.toml and fill in your actual database credentials
# DO NOT commit secrets.toml to version control - it should be in .gitignore

[postgres]
host = "your-database-host.com"
port = 5432
database = "hedis_portfolio"
user = "your-database-user"
password = "your-database-password"

# Example for local development:
# [postgres]
# host = "localhost"
# port = 5432
# database = "hedis_portfolio"
# user = "hedis_api"
# password = "hedis_password"

# For Streamlit Cloud:
# 1. Go to your app settings in Streamlit Cloud
# 2. Navigate to "Secrets" section
# 3. Paste the contents of this file (with actual values) into the secrets editor
# 4. Streamlit Cloud will automatically use these secrets at runtime


